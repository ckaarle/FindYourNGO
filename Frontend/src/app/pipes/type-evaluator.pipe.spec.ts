import { TypeEvaluatorPipe } from './type-evaluator.pipe';

describe('TypeEvaluatorPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeEvaluatorPipe();
    expect(pipe).toBeTruthy();
  });
});
